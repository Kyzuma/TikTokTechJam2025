import { useState } from '@lynx-js/react';

export function UserSettingsScreen() {
  return (
    <view className="Screen">
      <text className="ScreenTitle">User Settings</text>
      <text className="ScreenContent">Configure your app preferences.</text>
    </view>
  );
}
